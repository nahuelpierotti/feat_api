version: "3.8"

services:
  featdb:
    image: mysql:5.7
    environment:
      MYSQL_USER: 'root'
      MYSQL_ROOT_PASSWORD: "root"
    ports:
      - "9000:3400"
    volumes:
      - "mysql:/var/lib/mysql"

volumes:
  mysql: